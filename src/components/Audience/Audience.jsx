import { ArrowRight, Cpu, FileKey2, ShieldCheck } from 'lucide-react';
import './Audience.css';
const cards=[['ciso',<ShieldCheck/>, 'For CISOs','Understand organizational exposure, communicate risk and track quantum readiness at a strategic level.'],['engineering',<Cpu/>, 'For Engineering','Find vulnerable algorithms, dependencies and services and understand what needs to change.'],['procurement',<FileKey2/>, 'For Procurement','Ask vendors the right cryptographic questions and build quantum-safe requirements into technology decisions.']];
export default function Audience(){return <section className="audience-section"><div className="section-heading center-heading"><div className="section-tag"><span/>BUILT FOR THE PEOPLE WHO MOVE SYSTEMS</div><h2>One cryptographic picture.<br/><span>Different decisions.</span></h2></div><div className="audience-grid">{cards.map(([id,icon,title,text])=><article id={id} className="audience-card" key={id}><div className="audience-icon">{icon}</div><h3>{title}</h3><p>{text}</p><button onClick={()=>document.getElementById('platform')?.scrollIntoView({behavior:'smooth'})}>Explore<ArrowRight size={15}/></button></article>)}</div></section>}
